spring:
  datasource:
    url: jdbc:mysql://classiverse-prod-db.cd4i4aicm850.ap-northeast-2.rds.amazonaws.com:3306/classiverse_prod
    username: admin
    password: ${PROD_DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

jwt:
  secret: ${JWT_SECRET}
  expiration: 3600000