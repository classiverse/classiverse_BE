spring:
  datasource:
    url: jdbc:mysql://엔드포인트주소:3306/classiverse_prod
    username: admin
    password: ${PROD_DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

jwt:
  secret: ${JWT_SECRET}
  expiration: 3600000